name: Build and Package

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:
    # 手动触发构建

env:
  GRADLE_OPTS: "-Dfile.encoding=UTF-8"

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 23
      uses: actions/setup-java@v4
      with:
        java-version: '23'
        distribution: 'temurin'
        
    - name: Cache Gradle dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Check CloudSim Plus dependency
      run: |
        echo "⚠️  注意: 项目依赖 CloudSim Plus 9.0.0-SNAPSHOT (本地构建版本)"
        echo "如果构建失败，可能需要先构建 CloudSim Plus 并安装到本地 Maven 仓库"
        echo "或者修改 build.gradle.kts 使用已发布的 CloudSim Plus 版本"
        
    - name: Build with Gradle
      run: ./gradlew clean build --no-daemon
      env:
        GRADLE_OPTS: "-Dfile.encoding=UTF-8"
        
    - name: Build fat JAR
      run: ./gradlew fatJar --no-daemon
      env:
        GRADLE_OPTS: "-Dfile.encoding=UTF-8"
      
    - name: Upload JAR artifact
      uses: actions/upload-artifact@v4
      with:
        name: cloudsim-benchmark-jar
        path: build/libs/cloudsim-benchmark-*.jar
        retention-days: 30
        
    - name: Upload build reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: build-reports
        path: |
          build/reports/
          build/test-results/
        retention-days: 7

